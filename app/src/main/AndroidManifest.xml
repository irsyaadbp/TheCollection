<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.irsyaad.dicodingsubmission.thecollection">

   <uses-permission android:name="android.permission.INTERNET" />

   <application
           android:allowBackup="true"
           android:icon="@mipmap/ic_launcher"
           android:label="@string/app_name"
           android:roundIcon="@mipmap/ic_launcher_round"
           android:supportsRtl="true"
           android:theme="@style/AppTheme">
      <activity android:name=".ui.activity.FavoriteActivity"></activity>
      <activity android:name=".ui.activity.setting.LanguageActivity"></activity>
      <activity android:name=".ui.activity.setting.SettingActivity"></activity>
      <activity android:name=".ui.activity.detail.DetailTvActivity"></activity>
      <activity android:name=".ui.activity.detail.DetailFilmActivity" />
      <activity android:name=".ui.activity.MainActivity">
         <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
      </activity>
   </application>

</manifest>